# 2026-08-23 日志

## 今天干了什么

### 1. 设计访谈（grilling，逐项确认）
围绕"个人像素风网站"完成了 decision tree 的全部关键分支：

| 决策 | 结论 |
|------|------|
| 目标定位 | C：边做边学，够用复杂度 |
| 部署方式 | 暂不上线，本地优先（后定） |
| 技术栈 | 纯 HTML/CSS/JS，零构建工具 |
| 网站结构 | 多页 → 被"开场 + 菜单"单页交互取代 |
| 视觉风格 | D：整体如像素画（非游戏 UI） |
| 素材来源 | 代码生成像素图形为主（JS 字符串数组 + pixelSvg 渲染） |
| 配色 | 柔和低饱和（**正式色板待定**，当前为临时占位） |
| 字体 | BigBlueTerm437 标题 + Mononoki 正文 + 系统黑体中文兜底 |
| 动效 | 轻量 CSS + 1~2 个招牌动效 |
| 移动端 | 只需"能看"级别 |
| 开场交互 | 居中头像 + 网名 + 闪烁"点击任意处开始" → 点击后头像右移、左侧浮现菜单（欢迎/选项二/选项三） |

### 2. 原型制作（prototype skill）
- 完成 `index.html`：3 个 UI 变体（A 经典标题菜单 / B 像素窗口面板 / C 对话选项），`?variant=a|b|c` + 底部浮动栏 + ←/→ 键盘切换
- 字体复制至 `fonts/`（含 LICENSE），本地服务器 `python3 -m http.server 8000` 已启动
- 像素素材全部代码生成：占位像素头像（16×16 像素小人）、漂移像素云、菜单小图标
- **用户尚未反馈变体观感**

### 3. 工程化落地
- 创建 `AGENTS.md`（运行/验证命令、结构、设计决策、约定）
- 创建 `obsidian_web/`（笔记约定）+ `obsidian_web/日志/`（日志约定）
- git 初始化（main 分支），GitHub public 仓库 **fghmnst/web_test** 创建并推送首个 commit `8942f4e`（SSH 协议）

## 明天该干什么

1. **反馈原型**：在浏览器查看 3 个变体（http://localhost:8000/index.html），确定胜出变体（或组合，如"A 的头像 + C 的对话框"）
2. **折进正式实现**：将胜出变体固化为正式页面，移除变体切换条与落选变体
3. **用户提供素材**：网名、正式配色、爱好列表（2~3 个）、头像形象
4. **实现菜单行为**：点击"欢迎/选项二/选项三"后的内容切换（内容待定）
5. **收尾项**：字体子集化（上线前）、git 提交 AGENTS.md 更新

## 待用户决定
- 3 个变体的取舍
- 正式配色、网名、头像
- 爱好内容与菜单选项行为
- 部署方式（暂缓）
